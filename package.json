{
  "name": "wabao-ai",
  "version": "0.1.0",
  "private": true,
  "description": "蛙宝 AI 工作台 —— 文、图、声一体的多模态 AI 工作台（P1 文本阶段）",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "db:up": "cd apps/api && docker compose up -d",
    "db:down": "cd apps/api && docker compose down",
    "db:init": "pnpm --dir packages/shared build && pnpm --dir apps/api prisma:deploy && pnpm --dir apps/api seed",
    "db:baseline": "pnpm --dir apps/api prisma:baseline",
    "db:status": "pnpm --dir apps/api prisma:status",
    "db:studio": "pnpm --dir apps/api exec prisma studio",
    "free:ports": "node scripts/free-port.mjs 3001 5173 5174",
    "shared:build": "pnpm --dir packages/shared build",
    "dev:api": "node scripts/free-port.mjs 3001 && pnpm --dir packages/shared build && pnpm --dir apps/api start:dev",
    "dev:web": "node scripts/free-port.mjs 5173 && pnpm --dir packages/shared build && pnpm --dir apps/web dev",
    "test": "pnpm --dir apps/api test",
    "test:e2e": "pnpm --dir apps/api test:e2e",
    "build": "pnpm --dir packages/shared build && pnpm --dir apps/api build && pnpm --dir apps/web build"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.19.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "esbuild",
      "@nestjs/core"
    ]
  }
}
